---
# NFS-Konfiguration für Paperless-NGX
# Diese Datei enthält NAS-spezifische Einstellungen
apiVersion: v1
kind: ConfigMap
metadata:
  name: paperless-nfs-config
  namespace: paperless-ngx
data:
  nfs-server: "192.168.68.126"
  consume-path: "/volume1/scans"
  export-path: "/volume1/Privat/DATA"
  nfs-version: "3"
